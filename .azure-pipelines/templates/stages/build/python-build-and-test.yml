# python-build-and-test.yml
parameters:
  - name: pythonVersion
    type: string
    default: '3.11'

  - name: requirementsFile
    type: string
    default: 'requirements.txt'

  - name: coverageReportPath
    type: string
    default: 'coverage.xml'

  - name: testResultsPath
    type: string
    default: 'junit/test-results.xml'

  - name: archiveFileName
    type: string
    default: '$(Build.ArtifactStagingDirectory)/$(Build.SourceVersion).zip'

  - name: artifactName
    type: string
    default: 'Build-Drop'

  - name: projectPath
    type: string
    default: '.'
    # Path to the project root where requirements.txt is located

  - name: archiveSourcePath
    type: string
    default: '.'

  - name: archiveExclusions
    type: object
    default:
      - '**/.azure-pipelines/**'
      - '**/.github/**'
      - '**/.git/**'
      - '**/__pycache__/**'
      - '**/*.pyc'
      - '**/.pytest_cache/**'
      - '**/.coverage'
      - '**/venv/**'
      - '**/node_modules/**'

  - name: isContainerized
    type: boolean
    default: false

  - name: Dockerfile
    type: string
    default: '**/Dockerfile'

  - name: dockerBuildContext
    type: string
    default: '.'

  - name: dockerTags
    type: string
    default: 'latest'


stages:
- stage: BuildStage
  displayName: "Stage 2 - Build & Test"
  jobs:
  - job: BuildJob
    displayName: Build & Test
    steps:
    - checkout: self
      fetchDepth: 1

    - task: UsePythonVersion@0
      displayName: Use Python ${{ parameters.pythonVersion }}
      inputs:
        versionSpec: ${{ parameters.pythonVersion }}

    - task: Bash@3
      displayName: Install dependencies into .python_packages/lib/site-packages
      inputs:
        targetType: inline
        script: |
          set -e
          PROJECT_PATH="${{ parameters.projectPath }}"
          python -m pip install --upgrade pip
          echo "Installing dependencies from: $PROJECT_PATH/${{ parameters.requirementsFile }}"
          pip${{ parameters.pythonVersion }} install --target $PROJECT_PATH/.python_packages/lib/site-packages -r $PROJECT_PATH/${{ parameters.requirementsFile }}

    - bash: |
        # expose filenames for Publish tasks (basename evaluated by shell)
        testResultsFileName=$(basename "${{ parameters.testResultsPath }}")
        coverageFileName=$(basename "${{ parameters.coverageReportPath }}")
        echo "##vso[task.setvariable variable=testResultsFileName]$testResultsFileName"
        echo "##vso[task.setvariable variable=coverageFileName]$coverageFileName"
      displayName: 'Set test and coverage filenames'

    - task: Bash@3
      displayName: Run Pytest with Coverage (gracefully handle no tests)
      inputs:
        targetType: inline
        script: |
          set -e
          PROJECT_PATH="${{ parameters.projectPath }}"
          mkdir -p "$(Build.ArtifactStagingDirectory)"
          if [ ! -d "$PROJECT_PATH/venv" ]; then
            python3 -m venv "$PROJECT_PATH/venv"
          fi
          source "$PROJECT_PATH/venv/bin/activate"
          pip install pytest pytest-cov
          echo "Running pytest from $PROJECT_PATH with PYTHONPATH=$PROJECT_PATH/.python_packages/lib/site-packages"
          PYTHONPATH="$PROJECT_PATH/.python_packages/lib/site-packages:$PYTHONPATH" pytest -q --junitxml="$(Build.ArtifactStagingDirectory)/$(testResultsFileName)" \
            --cov="$PROJECT_PATH" --cov-report=xml:"$(Build.ArtifactStagingDirectory)/$(coverageFileName)" || exit_code=$?
          if [ "${exit_code:-0}" -eq 5 ]; then
            echo "No tests collected"
            echo '<testsuite name="no-tests" tests="0" errors="0" failures="0" skipped="0"></testsuite>' > "$(Build.ArtifactStagingDirectory)/$(testResultsFileName)"
            echo '<?xml version="1.0"?><coverage></coverage>' > "$(Build.ArtifactStagingDirectory)/$(coverageFileName)"
          elif [ "${exit_code:-0}" -ne 0 ]; then
            echo "Pytest failed with exit code ${exit_code}"
            exit ${exit_code}
          fi

    - task: PublishTestResults@2
      inputs:
        testResultsFormat: 'JUnit'
        testResultsFiles: '$(Build.ArtifactStagingDirectory)/$(testResultsFileName)'
        failTaskOnFailedTests: true
      condition: succeededOrFailed()
      displayName: 'Publish Test Results to Azure DevOps'

    - task: PublishCodeCoverageResults@2
      inputs:
        codeCoverageTool: 'Cobertura'
        summaryFileLocation: '$(Build.ArtifactStagingDirectory)/$(coverageFileName)'
      condition: succeededOrFailed()
      displayName: 'Publish Code Coverage to Azure DevOps'

    - task: Bash@3
      displayName: 'Collect test and coverage artifacts (ensure names)'
      inputs:
        targetType: inline
        script: |
          mkdir -p "$(Build.ArtifactStagingDirectory)"
          echo "Artifacts in $(Build.ArtifactStagingDirectory):"
          ls -la "$(Build.ArtifactStagingDirectory)" || true

    - task: Bash@3
      displayName: 'Copy app files into .python_packages/lib/site-packages'
      inputs:
        targetType: inline
        script: |
          set -e
          PROJECT_PATH="${{ parameters.projectPath }}"
          echo "Copying app files into $PROJECT_PATH/.python_packages/lib/site-packages"
          rsync -av --exclude='.python_packages' --exclude='.venv' --exclude='venv' --exclude='.git' --exclude='__pycache__' --exclude='.pytest_cache' "$PROJECT_PATH/" "$PROJECT_PATH/.python_packages/lib/site-packages/"
          echo "App files copied"

    - task: ArchiveFiles@2
      displayName: 'Archive project with .python_packages for Deployment'
      inputs:
        rootFolderOrFile: '$(System.DefaultWorkingDirectory)/${{ parameters.projectPath }}'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '${{ parameters.archiveFileName }}'
        replaceExistingArchive: true

    - task: PublishPipelineArtifact@1
      displayName: 'Publish Artifact: ${{ parameters.artifactName }}'
      inputs:
        targetPath: '$(Build.ArtifactStagingDirectory)'
        artifact: '${{ parameters.artifactName }}'
        publishLocation: 'pipeline'
